spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: api-gateway
  # the Config Server now provides All other configuration.
  # Keep this file minimal to avoid local overrides.
